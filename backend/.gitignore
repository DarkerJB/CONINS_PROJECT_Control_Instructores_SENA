# Dependencias
node_modules/
dist/

# Variables de entorno - NUNCA commitear
.env
.env.local
.env.*.local
.env.bak*
*.bak

# Datos reales del CDMC - NUNCA commitear
seed_data.sql

# Logs
*.log
npm-debug.log*

# Sistema
.DS_Store
*.tsbuildinfo

# Claude Code
.claude

# Nota: seed_maestro.sql (catalogos base + admin, NO datos reales) SI se versiona:
# lo necesita `npm run db:setup` en el despliegue. Los datos reales van en seed_data.sql (ignorado).
